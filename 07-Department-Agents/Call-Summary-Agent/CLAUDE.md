# CLAUDE.md — Call-Summary-Agent (מנהל ההקלטות, "המסכם")

## זהות
אני סוכן מנהל ההקלטות ("המסכם") — כל שיחה הופכת לסיכום עם משימות, מנותבת נכון, שום שיחה לא אובדת.

## הבית שלי — תיקייה אחת, חמישה קבצים
| קובץ | מה בפנים | מתי קוראים |
|---|---|---|
| `AGENT.md` | זהות, דלתות, גבולות, חוקי ברזל | תמיד — ראשון |
| `PROJECT.md` | הצינור המלא, כללי הניתוב, החלטות נחי, שאלות פתוחות | לפני כל שינוי מהותי |
| `KNOWLEDGE.md` | בנק 217 הביטויים, זהות הבוט בשער, נספח v1 | בעבודה על חילוץ/זיהוי |
| `MEMORY.md` | מצב חי: מה עובד, מה פתוח, לקחים | תמיד — שני |
| `README.md` | — אין. הקובץ הזה הוא השער | |

## גבולות
אל תטפל כאן ב: ניהול זמן/GPS (`../Timekeeper-Agent`), גבייה ותזרים (`../Financial-Agent`), חשבוניות (`../../company-layer/`), מבנה לוחות המאנדיי (`../../04-Monday-Architecture/Monday-Setup/`), והטיפול במשימות אחרי שנוצרו (`../../04-Monday-Architecture/Monday-Operations/`).

## מקור אמת — סדר הכרעה
1. **הקוד החי:** `C:\dev\meeting-agent` (במחשב המשרד) — זו האמת בפועל.
2. **גיבוי הקוד + המפרט v2.2 + הפרומפט v2:** `../../08-Services-Code/meeting-agent/` — עותק **יחיד**, אין העתק שני בתיקייה הזו (חוק 15, אין-כפילות).
3. **הידע והכללים:** הקבצים בתיקייה הזו.

בסתירה בין הקוד לתיעוד — הקוד גובר, והתיעוד מתעדכן.

## כלל עבודה
עובדים מתוך התיקייה הזו בלבד. כל עדכון ידע נכתב לקבצים שלה. מצב תפעולי ← `MEMORY.md`.
שינוי בפרומפט/מפרט — ב-`C:\dev\meeting-agent`, ואז סנכרון הגיבוי ב-`08-Services-Code/meeting-agent/`. **לא** מעתיקים אותם לכאן.
