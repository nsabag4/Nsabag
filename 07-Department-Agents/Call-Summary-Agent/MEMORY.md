# MEMORY.md — סוכן מנהל ההקלטות | זיכרון חי

> **קרא אותי שני, אחרי `AGENT.md`.** כאן המצב בפועל — לא הכללים (אלה ב-`PROJECT.md`).
> עדכון אחרון: 11.08.2026 (איחוד התיקייה). מצב תפעולי אחרון שנרשם: 17.07.2026.

---

## 1. שורה תחתונה — מה המצב

| | |
|---|---|
| **מעמד** | סוכן מן המניין (הועלה 12–17.07.2026, הנחיית נחי). לא אחד מ-13 סוכני המאנדיי — אחד משלושת **המנועים** (ראה `00-Constitution/ZONES.md`) |
| **חי?** | ✅ עובד. הסיכום האחרון שנרשם: **16.07.2026** |
| **הקוד** | `C:\dev\meeting-agent` (מחשב המשרד) · גיבוי בגיט: `../../08-Services-Code/meeting-agent/` |
| **הבוט** | @nahi_sabag_agent_bot (שער האישור וההודעות) |
| **בוט השאלות בשער** | gate-summarizer (`C:\dev\agent-gate\prompts\summarizer.md`) — הזהות ב-`KNOWLEDGE.md` §4 |

## 2. מה עובד בפועל

- **הצינור המלא:** הקלטה בטלפון ← Autosync ← Drive ← המחשב ← תמלול Groq ← חילוץ (`claude -p` + `prompt.md`) ← שער אישור בטלגרם ← כתיבה למאנדיי.
- **ROUTE_BY_CASE=1** — לקוח עם תיק: הסיכום נצמד לתיק (עיקרון בית החולים).
- **חוק אין-כפילות** — `find_duplicate_task` פעיל בקוד.
- **התאמת קבוצה לפי מזהה** ולא לפי שם (חסין לשינויי שם ידניים של נחי במאנדיי).
- **מגן היסטוריה** — `INBOX_MIN_DATE=2026-07-07`; הקלטות ישנות מדולגות ונרשמות.
- **סגירת מעגל** — `close_handled_callbacks` מוריד חזרות שטופלו מרשימת שומר-הזמן.

## 3. פתוח — לפי סדר חשיבות

| # | מה | תלוי במה | מצב |
|---|---|---|---|
| 1 | **הניתוב המלא:** פגישה ← יומן גוגל · חזרה ← Any.do · כספי ← בוט פיננסי | הרשאת **כתיבה** ליומן גוגל (נחי) + בחירת לוח חדר-מיון תפעולי | מתוכנן, לא נבנה |
| 2 | **הצינור הישן** ("Personal reminder file", לולאה בסשן) — האם נסגר? | אימות במחשב | ⚠️ סיכון עיבוד כפול |
| 3 | **תמלול עברי מקומי** (Whisper משבש עברית בשיחות רועשות) | שדרוג שלב 3 במפרט | מתוכנן |
| 4 | **לוח חדר המיון התפעולי** — 5094204623 הישן, או אחר? | הכרעת נחי | פתוח |
| 5 | **סוג השיחה השלישי** שנחי ביקש (סיכום / תזכורת / פיננסית?) | אישור נחי | פתוח |
| 6 | **שורת סיכום יומית ללוח הבוקר** (שלב 4) | — | מתוכנן |
| 7 | אופטימיזציית סוללה + סנכרון סלולרי לאפליקציית הסנכרון בטלפון | אישור נחי | לא אומת |

**נסגר:** ~~הקוד לא בגיט~~ — גובה ב-`08-Services-Code/meeting-agent/` (קומיט `291f10f`). הסיכון שנרשם ב-`SYSTEM-PROVENANCE.md` כבר לא רלוונטי למסכם.

## 4. לקחים תפעוליים — נשרפנו, לא לחזור

1. **אל תהרוג את השירות ב-force באמצע עיבוד** — עלול לאבד `state.json` ואיתו את ה-`chat_id` (קרה 09.07: הסוכן לא יכל לשלוח כלום).
2. **ריסטארט מרובה יוצר עיבוד חוזר** — `processed_files` נרשם רק בסיום מלא.
3. **שינוי שם קבוצה במאנדיי שובר כתיבה** — קרה כשנחי שינה את שם "🎙️ סיכומי שיחות". תוקן: התאמה לפי מזהה קבוצה.
4. **הבטחה שעברה ⇦ לא לנדנד** — נודניק על הבטחה ישנה נדנד 6 פעמים על לא-רלוונטי. תוקן: `NUDGE_MAX_STALE_MIN=30`.
5. **נחי סירב למחיקת פריטי בדיקה** מהלוחות ("לא מאשר מחיקה") — לא להציע שוב.

## 5. כשמשהו נשבר — סדר האבחון

1. **יומני השירות:** `C:\dev\meeting-agent\logs\` (קובץ ליום, בעברית).
2. **האם התהליך רץ:** `powershell "Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'meeting-agent' } | Select ProcessId, Name"`.
3. **לא רץ?** `start-agent.bat` או קיצור ההפעלה בתיקיית ההפעלה.
4. **הקלטות נערמות?** בדוק את `G:\האחסון שלי\DriveSyncFiles` (סנכרון מהטלפון) ואת `INBOX_PATH`.
5. **עריכת `prompt.md` לא דורשת ריסטארט** (נטען בכל חילוץ); שינוי `main.py` — כן דורש.

## 6. יומן שינויים

| תאריך | מה קרה |
|---|---|
| 07.07.2026 | הצינור נבנה והופעל מקצה-לקצה; פרומפט v2 עם מילון 217 הביטויים |
| 09.07.2026 | 4 באגים תוקנו (שם קבוצה · chat_id · נודניק מאחר · עיצוב); ROUTE_BY_CASE; מפרט הניתוב המלא; חוק אין-כפילות. תור של 8 הקלטות שנתקעו שוחרר |
| 11.07.2026 | התיעוד רוכז לגיט מחומרי הקליטה |
| 12–17.07.2026 | הועלה למעמד סוכן מן המניין |
| 16.07.2026 | הסיכום האחרון שנרשם ✅ |
| 11.08.2026 | **איחוד:** `05-AI-Framework/Call-Recording/` בוטל; כל הידע עבר לתיקייה הזו. הפרומפט והמפרט הושארו בעותק יחיד ב-`08-Services-Code/meeting-agent/` (הוסרה כפילות בייט-לבייט) |
