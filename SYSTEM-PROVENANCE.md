# מפת המקור — מה נבנה איפה (Sabag360)

> נכתב לבקשת נחי, 17.07.2026. מטרה: להבחין בבירור בין **הקוד שרץ בפועל** (נבנה בשיחות מוקדמות, חי ב-`C:\dev`) לבין **התיעוד והמסגרת** (נבנו בשיחת המסגרת הזו, חיים בגיט).
>
> **עיקרון:** כל שיחת קלוד רואה רק את עצמה. הקובץ הזה הוא הזיכרון החוצה-שיחות — כדי שלא צריך "לגלול" כדי לדעת מי בנה מה.

## הכלל הגדול

| שכבה | נבנתה ב… | חיה ב… |
|---|---|---|
| **הקוד שרץ** (השירותים בפועל) | שיחות מוקדמות (07–09.07.2026) | `C:\dev` במחשב — **לא בגיט** |
| **התיעוד + המסגרת** (סוכנים, חוקה, מרכז מאנדיי) | שיחת המסגרת הזו (11–17.07.2026) | גיט: `nsabag4/Nsabag` |
| **היסטוריית השיחות** (חומר גלם) | כל השיחות | גיט: `99-Intake/` |

## פירוט לפי מערכת

### 🎙️ הקלטת שיחות, תמלול וסיכום — לא נבנה בשיחה הזו
- **הקוד (שיחה מוקדמת):** `C:\dev\meeting-agent` — שירות פייתון: תמלול Groq, חילוץ, שער אישור בטלגרם, כתיבה למאנדיי. בוט: @nahi_sabag_agent_bot.
- **התיעוד (השיחה הזו):** `05-AI-Framework/Call-Recording/`.
- **היסטוריית הבנייה:** `99-Intake/meeting-agent-call-summaries/` (כולל main.py, פרומפט v2, בנק 217 ביטויים).

### ⏰ ניהול זמן (GPS + Wi-Fi + בלוטות') — לא נבנה בשיחה הזו
- **הקוד (שיחה מוקדמת):** `C:\dev\tizkoran` — שירות פייתון, פורט 8787: TomTom, בלוטות' רכב, התראות יציאה. בוט: @nahi_tizkoran_bot.
- **התיעוד (השיחה הזו):** `06-Development/Smart-Time/`.
- **היסטוריית הבנייה:** `99-Intake/timekeeper-role-definition/` + `99-Intake/server-bringup-session/`.

### 🤖 שער הסוכנים — לא נבנה בשיחה הזו
- **הקוד (שיחה מוקדמת):** `C:\dev\agent-gate` — המאזין לבוטים, כלב שמירה, מראה פיננסית.
- **היסטוריה:** `99-Intake/sabag-agent-system/` + `99-Intake/server-bringup-session/`.

### 🏗️ הקמת המאנדיי + הסוכנים המחלקתיים — חלקם בשיחה הזו
- **הקמת החשבון (שיחות מוקדמות):** לוחות, עמודות, אוטומציות, סוכני מאנדיי — היסטוריה ב-`99-Intake/monday-agent-system/` ו-`sabag-monday-agent-system/`.
- **המסגרת והסוכנים המחלקתיים (השיחה הזו):** `04-Monday-Architecture/`, `07-Department-Agents/`, `00-Constitution/`.

## מה שנבנה **בשיחה הזו** במדויק (11–17.07.2026)
- החוקה (`00-Constitution`), מרכז המאנדיי (`04`), תיעוד 4 הפרויקטים (`04/05/06`), 9 סוכני מחלקות + מנכ"ל (`07`), ריכוז כל היסטוריית השיחות ל-`99-Intake`.
- תוכניות (עוגנו כאן, בוצעו בסשן מקומי במחשב): רפלקסים בלוח (`WEBHOOK-REFLEXES-PLAN`, שלב א' בוצע 13.07), אמינות שירותים (`SERVICES-RELIABILITY-PLAN`, בוצע 13.07).

## מה **לא** נבנה בשיחה הזו
- הקוד של: הקלטת שיחות/תמלול/סיכום · ניהול זמן · שער הסוכנים · הבוטים הפיננסי/מסכם/שומר-זמן · הקמת חשבון המאנדיי הראשונית. כל אלה — שיחות מוקדמות, קוד ב-`C:\dev`.

## הפער הפתוח
קוד `C:\dev` (meeting-agent, tizkoran, agent-gate) **עדיין לא בגיט** — קיים רק במחשב. סיכון: נפילת מחשב = אובדן הקוד הרץ (התיעוד יישאר). ריכוזו לגיט = משימה פתוחה.
