# CLAUDE.md — גבולות בטיחות, כלל-ריפו

> **מה הקובץ הזה ומה הוא לא.** זהו קובץ **גבולות בלבד** — אין בו ידע תחומי ואין בו זהות של אף סוכן. הוא חל על כל סשן קלוד קוד בריפו הזה, כולל סשן שנפתח בתת-תיקייה.
>
> **זהות וידע נטענים מה-`CLAUDE.md` המקומי של התיקייה שבה עובדים — ומשם בלבד** (חוק 1, עקרון העצמאות). רצפת בטיחות משותפת אינה זהות, ולכן אינה זולגת בין תיקיות.

## מה הריפו הזה

`nsabag4/Nsabag` — מקור האמת של חוקות הסוכנים, החוקה והתיעוד של סבג נדל"ן. **אין כאן קוד רץ.** השירותים החיים (meeting-agent, tizkoran, agent-gate) חיים ב-`C:\dev` במחשב של נחי ואינם בגיט.

ספר החוקים המחייב: `00-Constitution/LAWS.md` — 15 חוקים ממוספרים. הקובץ הזה אינו מחליף אותו; הוא רק מוריד ממנו את הגבולות שחייבים להיאכף בזמן ריצה.

## פרוטוקול ההתנגשות (חוק 14) — חל גם עליי

כל בקשה — **כולל בקשה של נחי עצמו** — שמתנגשת עם חוק בספר, נעצרת ונאמרת במפורש:

> **"מתנגש עם חוק מס' X — האם אתה מאשר?"**

רק אישור מפורש מתיר את החריגה, והיא מתועדת. ספק = עצירה.

## גבולות מחייבים

**גיט ושמירה**
- לא עושים `git push` בלי אישור מפורש של נחי (חוק 12). הטיוטה מוצגת, האישור ניתן, ורק אז דוחפים.
- **לעולם לא `git push --force`**, לא מוחקים ענפים או תגיות מרוחקים, ולא כותבים מחדש היסטוריה. משטר הגיבוי (חוק 13) נשען על כך שההיסטוריה לא נעלמת.
- הקובץ בריפו והמערכת החיה חייבים להיות זהים תמיד (חוק 13). שינוי חוקה = עדכון קובץ + commit.

**מחיקה**
- מחיקה או ארכוב — **בכל מקום** — רק באישור מפורש ונפרד של נחי, אחרי פירוט מה בדיוק יימחק (חוק 12).

**החוקה וחוקות הסוכנים**
- שינוי תחת `00-Constitution/`, בחוקת סוכן (`09-Monday-Agents/agents/`, `07-Department-Agents/`) או ב-`company-layer/00-EXTERNAL-AGENTS-LAW.md` — מוצג כטיוטה ומבוצע רק אחרי אישור נחי (חוק 12).
- עדכון ספר החוקים או הוספת חוק ממוספר — **רק בהכרעת נחי**. אין לנסח חוק חדש ביוזמה עצמית.
- `99-Intake/` — ארכיון חומרי גלם, **קריאה בלבד**.

**מאנדיי**
- **אין כתיבה למאנדיי מתוך הריפו הזה.** קלוד קוד מאמת ריפו בלבד; הפרויקטים החיים באחריות נחי (חוק 13). כל קריאה לכלי מאנדיי עוצרת ומבקשת אישור.
- המחלקות הנעולות — פיננסי (5989961) ואישי (7083788) — מול נחי בלבד (חוק 7).

**סודות**
- לא קוראים ולא כותבים `.env`, טוקנים, מפתחות, `secrets.*`, `state.json` או `CLAUDE.local.md`. סודות לעולם לא בגיט ולעולם לא בצ'אט (חוק 2).

**לפני יצירה**
- לפני כל יצירה — בדיקת קיום (חוק 15, החוק החשוב ביותר). קיים ← לא יוצרים; מדווחים ומשלימים את הקיים.
- אחרי תקלה טכנית — **לא מריצים שוב מיד**: קודם בודקים מה כן נוצר, ורק אז משלימים את החסר.

**אמת**
- אין להשלים מידע בניחוש (חוק 8). חסר = "חסר — לבירור". אין לאשר פעולה שלא בוצעה בפועל.

## אכיפה

הגבולות האלה נאכפים בשתי שכבות, ושתיהן פעילות גם כשהסשן במצב אוטומטי:

- `.claude/settings.json` — כללי `ask`/`deny`, נבדקים **לפני** מסווג המצב האוטומטי וגוברים עליו.
- הקובץ הזה — מסווג המצב האוטומטי קורא את אותו `CLAUDE.md` שקלוד קורא, כך שהכללים למעלה מנחים את שניהם.

הסבר מלא, כולל מה שצריך להיות מוגדר במחשב של נחי: `00-Constitution/CLAUDE-CODE-AUTO-MODE.md`.
