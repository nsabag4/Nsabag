# תוכנית אמינות השירותים — שלא ייפלו יותר בשקט

> הבעיה (מתועדת, 09.07.2026): שלושת השירותים במחשב המשרד — שער הסוכנים (agent-gate), המסכם (meeting-agent), שומר הזמן (tizkoran) — נפלו בלילה ולא קמו לבד. תוצאה: 3 ימי דממה בצינורות בלי שאף אחד ידע.
> הפתרון: הפיכתם לשירותים שקמים אוטומטית — אחרי קריסה, אחרי הפעלה מחדש, תמיד.

## היעד

1. **קימה אוטומטית אחרי קריסה:** שירות שנפל חוזר לבד תוך דקה.
2. **קימה אוטומטית אחרי הפעלת מחשב:** בלי תלות בקיצורי Startup ובכניסת משתמש.
3. **ניקוי לפני קימה:** הריגת פייתון יתום שמחזיק את פורט 8787 (הלקח המתועד — בלעדיו הקוד הישן ממשיך לענות).
4. **התראה כפולה:** נפילה ⇦ כלב השמירה הקיים מתריע בטלגרם (קיים) + השירות מנסה לקום לבד (חדש).

## השיטה

**מנהל המשימות של Windows (Task Scheduler)** לכל שירות — בלי תוכנות חדשות:
- טריגר 1: בעליית המערכת (At startup) — בלי צורך בכניסת משתמש.
- טריגר 2: כל 5 דקות — בדיקה: התהליך חי? לא ⇦ ניקוי (פורט/יתומים) ⇦ הרצה מחדש דרך WMI (הלקח: תהליך שנפתח מסשן נהרג בסופו).
- כתיבת שורת יומן על כל קימה-מחדש (מתי, למה) לקובץ log — כדי שדוח הבוקר יוכל לספר "השירות X קם מחדש הלילה".

## פרומפט הביצוע — לסשן מקומי במחשב המשרד

```
קרא את C:\Sabag360-Framework\06-Development\SERVICES-RELIABILITY-PLAN.md ובצע אותו במדויק:
1. בדוק את המצב הנוכחי של שלושת השירותים (agent-gate, meeting-agent, tizkoran) — מי חי ומי למטה. מי שלמטה — הרם עכשיו (עם ניקוי פורט 8787 מיתומים).
2. צור משימות מתוזמנות (Task Scheduler): לכל שירות — טריגר עליית מערכת + בדיקת דופק כל 5 דקות עם קימה-מחדש אוטומטית וניקוי לפני.
3. כל קימה-מחדש נרשמת ל-log ייעודי (C:\dev\service-restarts.log).
4. בדוק קצה-לקצה: הרוג שירות אחד בכוונה — וודא שקם לבד תוך 5 דקות ושכלב השמירה התריע.
5. בדוק גם: אפליקציית הסנכרון בטלפון (Autosync) — אם אין קבצים חדשים מהטלפון מאז 09.07, הנחה את נחי לבדוק את אופטימיזציית הסוללה (האזהרה האדומה המתועדת).
6. עדכן את MEMORY.md של הפרויקטים הרלוונטיים + הקובץ הזה (סטטוס: בוצע) + commit + push.
כללים: אל תהרוג את המסכם באמצע עיבוד הקלטה (הלקח: אובדן state). עברית נקייה. תקלה — דווח ועצור.
```

## סטטוס

**בוצע (13.07.2026):** watchdog v2 ב-C:\dev (תזכורן חי = פורט 8787; לעולם לא הורג שירות חי) + משימות דופק כל 5 דק' לשלושת השירותים + יומן קימות ב-C:\dev\service-restarts.log. קימה אוטומטית אומתה בשטח. פתוח (משימה 3075145320 במנהל החכם): BIOS Restore-on-AC + כניסה אוטומטית + שומר-על מהענן. הערה: הדופק רץ בהקשר המשתמש — פעיל מרגע הכניסה למערכת.
